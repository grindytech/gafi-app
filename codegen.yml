schema: https://playground.gafi.network/
documents: './src/**/*.ts'
generates:
  ./src/graphQL/generates.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-query
    config:
      fetcher: graphql-request